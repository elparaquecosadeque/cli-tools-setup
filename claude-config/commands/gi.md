---
description: Grill me about a feature/decision, then build it with Impeccable once I confirm.
argument-hint: <what to grill about>
---

Run a `/grilling` session on the topic below (invoke the `mattpocock-skills:grilling` skill): interview me relentlessly one question at a time, walking the decision tree, giving your recommended answer each time, and wait for my reply before continuing. Do not implement anything until I explicitly confirm we've reached shared understanding.

Once I confirm the plan, invoke the `impeccable:impeccable` skill to design/build it — let impeccable's own routing decide how much design ceremony the task actually warrants (a small functional fix doesn't need the same treatment as a new surface).

Topic: $ARGUMENTS
